This is a prompt describing a project of mine. The project is a turret that can be piloted from a web interface.

The embedded firware is developped using the arduino framework and platoformio, the web server should be developped using flask, and the web interface is developped using react.

You will be in charge of structuring the project into a single repo, with one folder for the embedded firmware, one for the web server and one for the web interface, Then write your plan on how you would dockerize each applications

the frontend and firmware are already written so don't change them  !